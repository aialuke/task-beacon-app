/**
 * Auth Feature - Public API
 *
 * Authentication feature with lazy-loaded components and direct exports for hooks/utilities
 */

// === LAZY COMPONENTS (Code Splitting) ===
// TODO: Add auth components when they are implemented
// export const LoginForm = lazy(() => import('./components/LoginForm'));
// export const SignupForm = lazy(() => import('./components/SignupForm'));
// export const AuthModal = lazy(() => import('./components/AuthModal'));

// === DIRECT EXPORTS (Hooks & Utilities) ===
// TODO: Add auth hooks when they are implemented
// export { useAuth } from './hooks/useAuth';
// export { useAuthMutations } from './hooks/useAuthMutations';

// Types - TODO: Add when types are implemented
// export type * from './types';

// === FEATURE METADATA ===
export const FEATURE_NAME = 'auth';
export const FEATURE_VERSION = '1.0.0';
