/**
 * Profile Feature - Public API
 *
 * Profile feature with lazy-loaded components and direct exports for hooks/utilities
 */

// === LAZY COMPONENTS (Code Splitting) ===
// TODO: Add profile components when they are implemented
// export const ProfileForm = lazy(() => import('./components/ProfileForm'));
// export const ProfileSettings = lazy(() => import('./components/ProfileSettings'));
// export const ProfileAvatar = lazy(() => import('./components/ProfileAvatar'));

// === DIRECT EXPORTS (Hooks & Utilities) ===
// TODO: Add profile hooks when they are implemented
// export { useProfile } from './hooks/useProfile';
// export { useProfileMutations } from './hooks/useProfileMutations';

// Types - TODO: Add when types are implemented
// export type * from './types';

// === FEATURE METADATA ===
export const FEATURE_NAME = 'profile';
export const FEATURE_VERSION = '1.0.0';
